"use strict"

let services = require("services");

module.exports = {
  /**
  @param {array} polygons array of one or more polygons to add to the database
  */
  "addPolygons" : function* (polygons) {

  },
  /**
  @param {float} lon_0, lon_1, lat_0, lat_1 longitudinal and latitudinal ranges
  from which to retrieve a set of polygons
  @returns {array} polygons an array of polygons
  */
  "getPolygons" : function (lon_0, lon_1, lat_0, lat_1) {
    
  }
}
